# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)


# Dashboard pages
GET		/index						controllers.Application.index()		
GET		/eat						controllers.Application.eat()
GET		/playy						controllers.Application.playy()
GET		/work						controllers.Application.work()
GET		/namaste					controllers.Application.namaste()
GET 	/archivedEats				controllers.Application.archivedEats()
